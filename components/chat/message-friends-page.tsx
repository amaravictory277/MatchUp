"use client";
import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, MessageCircle, Plus } from "lucide-react";
import { useRouter } from "next/navigation";
import { createBrowserSupabaseClient } from "../../lib/supabase/client";
type Profile={id:string;display_name:string|null;username:string|null;avatar_path:string|null};type Chat={id:string;name:string;kind:string;lastAt:string|null};const nameOf=(p:Profile)=>p.display_name||p.username||"MatchUp Player";const initials=(p:Profile)=>nameOf(p).slice(0,2).toUpperCase();
export function MessageFriendsPage(){const router=useRouter();const supabase=useMemo(()=>createBrowserSupabaseClient(),[]);const [newFriends,setNewFriends]=useState<Profile[]>([]);const [chats,setChats]=useState<Chat[]>([]);const [showAllFriends,setShowAllFriends]=useState(false);const [showAllChats,setShowAllChats]=useState(false);const [loading,setLoading]=useState(true);const [busy,setBusy]=useState("");useEffect(()=>{let cancelled=false;const load=async()=>{const {data:a}=await supabase.auth.getUser();if(!a.user){router.push("/auth");return;}const uid=a.user.id;const {data:fr}=await supabase.from("friendships").select("user_id,friend_id").eq("status","accepted").or(`user_id.eq.${uid},friend_id.eq.${uid}`);const ids=((fr||[]) as any[]).map(r=>r.user_id===uid?r.friend_id:r.user_id);const profiles=ids.length?(await supabase.from("profiles").select("id,display_name,username,avatar_path").in("id",ids)).data||[]:[];const {data:memberships}=await supabase.from("chat_group_members").select("group_id,chat_groups(id,name,kind)").eq("user_id",uid);const groups=(memberships||[]).map((r:any)=>r.chat_groups).filter(Boolean) as any[];const privateGroups=groups.filter(g=>g.kind==="private");let messagedIds:string[]=[];if(privateGroups.length){const {data:all}=await supabase.from("chat_group_members").select("group_id,user_id").in("group_id",privateGroups.map(g=>g.id));messagedIds=privateGroups.map(g=>((all||[]) as any[]).find(r=>r.group_id===g.id&&r.user_id!==uid)?.user_id).filter(Boolean) as string[];}setNewFriends((profiles as Profile[]).filter(p=>!messagedIds.includes(p.id)));const chatRows:Chat[]=[];for(const g of groups){const {data:m}=await supabase.from("chat_messages").select("created_at").eq("group_id",g.id).order("created_at",{ascending:false}).limit(1).maybeSingle();if(m)chatRows.push({id:g.id,name:g.name,kind:g.kind,lastAt:m.created_at});}chatRows.sort((a,b)=>new Date(b.lastAt||0).getTime()-new Date(a.lastAt||0).getTime());if(!cancelled){setChats(chatRows);setLoading(false);}};void load();return()=>{cancelled=true;};},[router,supabase]);const message=async(p:Profile)=>{setBusy(p.id);const {data,error}=await supabase.rpc("get_or_create_private_chat",{p_friend_id:p.id});if(!error&&data)router.push(`/leaderboard?group=${data}`);setBusy("");};return <main className="min-h-screen bg-[#061120] px-4 pb-10 pt-5 text-white sm:px-6"><div className="mx-auto max-w-3xl"><header className="sticky top-0 z-20 -mx-4 flex items-center gap-3 border-b border-white/5 bg-[#061120]/95 px-4 py-4 backdrop-blur sm:-mx-6 sm:px-6"><button type="button" onClick={()=>router.back()} className="icon-button" aria-label="Back"><ArrowLeft size={18}/></button><div><p className="text-[10px] font-black uppercase tracking-[.16em] text-[#47a8ff]">Messaging</p><h1 className="text-2xl font-black">Message New Friends</h1></div></header>{loading?<div className="py-16 text-center text-sm text-[#7892ac]">Loading your conversations…</div>:<><section className="pt-7"><div className="mb-3 flex items-end justify-between"><div><h2 className="text-lg font-black">Message New Friends</h2><p className="mt-1 text-xs text-[#7892ac]">Friends you have not messaged yet.</p></div>{newFriends.length>5?<button type="button" onClick={()=>setShowAllFriends(v=>!v)} className="text-xs font-black text-[#70c1ff]">{showAllFriends?"Show Less":"See All"}</button>:null}</div>{newFriends.length?<div className="no-scrollbar flex gap-3 overflow-x-auto pb-2">{(showAllFriends?newFriends:newFriends.slice(0,5)).map(p=><div key={p.id} className="min-w-[155px] rounded-2xl border border-[#18365f] bg-[#071426] p-4"><div className="grid size-14 place-items-center overflow-hidden rounded-full bg-[#0b3154] text-sm font-black text-[#70c1ff]">{p.avatar_path?<img src={p.avatar_path} alt="" className="size-full object-cover"/>:initials(p)}</div><p className="mt-3 truncate text-sm font-black">{nameOf(p)}</p><p className="mt-1 truncate text-[11px] text-[#7892ac]">@{p.username}</p><button type="button" disabled={busy===p.id} onClick={()=>void message(p)} className="mt-4 flex w-full items-center justify-center gap-1.5 rounded-xl bg-[#167bd1] px-3 py-2.5 text-xs font-black disabled:opacity-50"><MessageCircle size={14}/>{busy===p.id?"Opening…":"Message"}</button></div>)}</div>:<div className="rounded-2xl border border-dashed border-[#18365f] p-6 text-center text-sm text-[#7892ac]">You have no unmessaged friends right now.</div>}</section><section className="mt-9"><div className="rounded-2xl border border-[#194b7c] bg-[#08182b] px-4 py-3"><h2 className="text-center text-sm font-black tracking-[.14em] text-[#9bd3ff]">MY CHATS</h2></div><div className="mt-3 space-y-2">{(showAllChats?chats:chats.slice(0,3)).map(c=><button key={c.id} type="button" onClick={()=>router.push(`/leaderboard?group=${c.id}`)} className="flex w-full items-center gap-3 rounded-2xl border border-[#18365f] bg-[#071426] p-4 text-left"><span className="grid size-11 place-items-center rounded-full bg-[#0b3154] text-[#70c1ff]"><MessageCircle size={18}/></span><span className="min-w-0 flex-1"><span className="block truncate text-sm font-black">{c.name}</span><span className="block text-[11px] text-[#7892ac]">{c.kind==="group"?"Group":"Conversation"}</span></span><span className="text-[10px] text-[#7892ac]">{c.lastAt?new Date(c.lastAt).toLocaleDateString():""}</span></button>)}{!chats.length?<p className="rounded-2xl border border-dashed border-[#18365f] p-7 text-center text-sm text-[#7892ac]">No chats yet.</p>:null}</div>{chats.length>3?<button type="button" onClick={()=>setShowAllChats(v=>!v)} className="mt-3 w-full rounded-xl border border-[#245b91] bg-[#0a2946] px-4 py-3 text-sm font-black text-[#9bd3ff]">{showAllChats?"Show Less":"See All"}</button>:null}</section><section className="mt-9"><div className="mb-3 flex items-center justify-between"><h2 className="text-lg font-black">MY GROUPS</h2><button type="button" onClick={()=>router.push("/leaderboard?create=group")} className="flex items-center gap-1.5 rounded-xl bg-[#167bd1] px-4 py-2.5 text-xs font-black"><Plus size={15}/>Create Group</button></div><p className="text-xs text-[#7892ac]">Create or manage groups from Chat.</p></section></>}</div></main>;}
