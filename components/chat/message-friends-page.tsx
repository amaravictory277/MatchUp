"use client";

import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, ChevronRight, Plus, Swords, Trophy } from "lucide-react";
import { useRouter, useSearchParams } from "next/navigation";
import { createBrowserSupabaseClient } from "../../lib/supabase/client";
import { MatchUpAvatar } from "../ui/matchup-avatar";

type Profile={id:string;display_name:string|null;username:string|null;avatar_path:string|null};
type Chat={id:string;name:string;kind:string;lastAt:string|null;preview:string;avatar?:Profile|null;lastSenderId?:string|null;unread:number};
type ReadyMatch={matchId:string;conversationId:string;opponent:Profile;createdAt:string;unread:number};
const nameOf=(p:Profile)=>p.display_name||p.username||"MatchUp Player";
const relative=(value:string|null)=>{if(!value)return "";const d=new Date(value);const mins=Math.max(0,Math.floor((Date.now()-d.getTime())/60000));if(mins<1)return "Just now";if(mins<60)return `${mins}m`;const hours=Math.floor(mins/60);if(hours<24)return `${hours}h`;if(hours<48)return "Yesterday";return d.toLocaleDateString([], {month:"short",day:"numeric"});};
const Badge=({count}:{count:number})=>count>0?<span className="grid min-w-5 place-items-center rounded-full bg-[#167bd1] px-1.5 py-1 text-[10px] font-black leading-none text-white">{count>99?"99+":count}</span>:null;

export function MessageFriendsPage(){
 const router=useRouter(); const params=useSearchParams(); const supabase=useMemo(()=>createBrowserSupabaseClient(),[]); const [friends,setFriends]=useState<Profile[]>([]); const [chats,setChats]=useState<Chat[]>([]); const [matches,setMatches]=useState<ReadyMatch[]>([]); const [loading,setLoading]=useState(true); const [busy,setBusy]=useState(""); const [showAll,setShowAll]=useState(false); const [pendingChallenges,setPendingChallenges]=useState(0);
 const rawTab=params.get("tab"); const tab=rawTab==="groups"?"groups":rawTab==="ready"?"ready":"friends";
 useEffect(()=>{let cancelled=false; const load=async()=>{const {data:auth}=await supabase.auth.getUser();if(!auth.user){router.push("/auth");return;}const uid=auth.user.id;
   const {data:fr}=await supabase.from("friendships").select("user_id,friend_id").eq("status","accepted").or(`user_id.eq.${uid},friend_id.eq.${uid}`);const ids=((fr||[]) as any[]).map(r=>r.user_id===uid?r.friend_id:r.user_id);const fp=ids.length?(await supabase.from("profiles").select("id,display_name,username,avatar_path").in("id",ids)).data||[]:[];
   const {data:memberships}=await supabase.from("chat_group_members").select("group_id,chat_groups(id,name,kind,image_path)").eq("user_id",uid);const groups=(memberships||[]).map((r:any)=>Array.isArray(r.chat_groups)?r.chat_groups[0]:r.chat_groups).filter(Boolean);const unique=groups.filter((g:any,i:number,a:any[])=>a.findIndex(x=>x.id===g.id)===i);
   const chatRows:Chat[]=[];for(const g of unique){const {data:msgs}=await supabase.from("chat_messages").select("id,body,created_at,sender_id,profiles(id,display_name,username,avatar_path)").eq("group_id",g.id).order("created_at",{ascending:false}).limit(100);const rows=(msgs||[]) as any[];const latest=rows[0];if(!latest&&g.kind!=="group")continue;const messageIds=rows.map(m=>m.id);let unread=0;if(messageIds.length){const {data:reads}=await supabase.from("chat_message_reads").select("message_id,user_id").in("message_id",messageIds);const readSet=new Set(((reads||[]) as any[]).filter(r=>r.user_id===uid).map(r=>r.message_id));unread=rows.filter(m=>m.sender_id!==uid&&!readSet.has(m.id)).length;}const avatar=latest?.profiles?(Array.isArray(latest.profiles)?latest.profiles[0]:latest.profiles):null;chatRows.push({id:g.id,name:g.kind==="private"?(avatar?nameOf(avatar):"Private chat"):g.name,kind:g.kind,lastAt:latest?.created_at||null,preview:latest?.body||"No messages yet",avatar,lastSenderId:latest?.sender_id||null,unread});}
   const {data:reqs}=await supabase.from("match_requests").select("id").eq("opponent_id",uid).eq("status","pending");
   const {data:matchRows}=await supabase.from("matches").select("id,player_a_id,player_b_id,created_at,match_conversations(conversation_id)").or(`player_a_id.eq.${uid},player_b_id.eq.${uid}`).order("created_at",{ascending:false}).limit(50);
   const opponentIds=((matchRows||[]) as any[]).map(m=>m.player_a_id===uid?m.player_b_id:m.player_a_id).filter(Boolean);const opponents=opponentIds.length?(await supabase.from("profiles").select("id,display_name,username,avatar_path").in("id",opponentIds)).data||[]:[];const opMap=new Map((opponents as Profile[]).map(p=>[p.id,p]));const ready:ReadyMatch[]=[];
   for(const m of (matchRows||[]) as any[]){const conversation=Array.isArray(m.match_conversations)?m.match_conversations[0]:m.match_conversations;const opponent=opMap.get(m.player_a_id===uid?m.player_b_id:m.player_a_id);if(!conversation?.conversation_id||!opponent)continue;const {data:msgs}=await supabase.from("chat_messages").select("id,sender_id").eq("group_id",conversation.conversation_id).order("created_at",{ascending:false}).limit(100);const mids=((msgs||[]) as any[]).map(x=>x.id);let unread=0;if(mids.length){const {data:reads}=await supabase.from("chat_message_reads").select("message_id,user_id").in("message_id",mids);const seen=new Set(((reads||[]) as any[]).filter(r=>r.user_id===uid).map(r=>r.message_id));unread=((msgs||[]) as any[]).filter(x=>x.sender_id!==uid&&!seen.has(x.id)).length;}ready.push({matchId:m.id,conversationId:conversation.conversation_id,opponent,createdAt:m.created_at,unread});}
   if(!cancelled){setFriends(fp as Profile[]);setChats(chatRows.sort((a,b)=>new Date(b.lastAt||0).getTime()-new Date(a.lastAt||0).getTime()));setMatches(ready);setPendingChallenges((reqs||[]).length);setLoading(false);}
 };void load();const channel=supabase.channel("message-friends-realtime").on("postgres_changes",{event:"*",schema:"public",table:"friendships"},()=>void load()).on("postgres_changes",{event:"*",schema:"public",table:"chat_group_members"},()=>void load()).on("postgres_changes",{event:"*",schema:"public",table:"chat_messages"},()=>void load()).on("postgres_changes",{event:"*",schema:"public",table:"chat_message_reads"},()=>void load()).on("postgres_changes",{event:"*",schema:"public",table:"matches"},()=>void load()).on("postgres_changes",{event:"*",schema:"public",table:"match_requests"},()=>void load()).subscribe();return()=>{cancelled=true;void supabase.removeChannel(channel);};},[router,supabase]);
 const message=async(p:Profile)=>{setBusy(p.id);const {data,error}=await supabase.rpc("get_or_create_private_chat",{p_friend:p.id});if(!error&&data)router.push(`/leaderboard?group=${data}`);setBusy("");};
 const privateUnread=chats.filter(c=>c.kind!=="group").reduce((n,c)=>n+c.unread,0);const groupUnread=chats.filter(c=>c.kind==="group").reduce((n,c)=>n+c.unread,0);const matchUnread=matches.reduce((n,m)=>n+m.unread,0);
 return <main className="min-h-screen bg-[#061120] px-4 pb-28 pt-5 text-white sm:px-6"><div className="mx-auto max-w-2xl">
  <header className="flex items-center gap-3 border-b border-white/5 pb-5"><button type="button" onClick={()=>router.back()} className="icon-button" aria-label="Back"><ArrowLeft size={18}/></button><div className="flex-1"><p className="text-[10px] font-black uppercase tracking-[.16em] text-[#47a8ff]">Messaging</p><h1 className="text-2xl font-black">{tab==="ready"?"Ready Match":"Friends"}</h1></div><button type="button" onClick={()=>router.push("/friends")} className="grid size-10 place-items-center rounded-xl bg-[#0b3154] text-[#70c1ff]" aria-label="Add friends"><Plus size={18}/></button></header>
  <div className="mt-6 grid grid-cols-3 rounded-2xl border border-[#18365f] bg-[#071426] p-1"><button type="button" onClick={()=>router.replace("/message-friends?tab=friends")} className={`flex items-center justify-center gap-1.5 rounded-xl px-2 py-3 text-xs font-black transition ${tab==="friends"?"bg-[#167bd1] text-white":"text-[#7892ac]"}`}>FRIENDS<Badge count={privateUnread}/></button><button type="button" onClick={()=>router.replace("/message-friends?tab=groups")} className={`flex items-center justify-center gap-1.5 rounded-xl px-2 py-3 text-xs font-black transition ${tab==="groups"?"bg-[#167bd1] text-white":"text-[#7892ac]"}`}>GROUPS<Badge count={groupUnread}/></button><button type="button" onClick={()=>router.replace("/message-friends?tab=ready")} className={`flex items-center justify-center gap-1.5 rounded-xl px-2 py-3 text-xs font-black transition ${tab==="ready"?"bg-[#167bd1] text-white":"text-[#7892ac]"}`}>READY MATCH<Badge count={matchUnread+pendingChallenges}/></button></div>
  {loading?<div className="py-16 text-center text-sm text-[#7892ac]">Loading your messages…</div>:tab==="groups"?<section className="mt-7"><div className="mb-3 flex items-center justify-between"><div><h2 className="text-[11px] font-black uppercase tracking-[.16em] text-[#70c1ff]">Groups</h2><p className="mt-1 text-xs text-[#7892ac]">Your active group conversations.</p></div><div className="flex items-center gap-2"><Badge count={groupUnread}/><button type="button" onClick={()=>router.push("/groups")} className="rounded-xl px-3 py-2 text-xs font-black text-[#9bd3ff]">View all</button></div></div>{chats.filter(c=>c.kind==="group").length?<div className="space-y-3">{chats.filter(c=>c.kind==="group").map(c=><button key={c.id} type="button" onClick={()=>router.push(`/leaderboard?group=${c.id}`)} className="flex w-full items-center gap-4 rounded-2xl border border-[#18365f] bg-[#071426] p-3 text-left"><MatchUpAvatar group profile={{id:c.id,display_name:c.name,avatar_path:null,username:null}} size="md"/><span className="min-w-0 flex-1"><span className="block truncate text-sm font-black">{c.name}</span><span className="mt-1 block truncate text-xs text-[#7892ac]">{c.preview}</span></span><span className="flex shrink-0 flex-col items-end gap-2"><span className="text-[10px] text-[#7892ac]">{relative(c.lastAt)}</span><Badge count={c.unread}/></span><ChevronRight size={18} className="text-[#5f86a8]"/></button>)}</div>:<div className="rounded-2xl border border-dashed border-[#18365f] p-8 text-center text-sm text-[#7892ac]">You are not in any groups yet.</div>}</section>:tab==="ready"?<section className="mt-7"><div className="mb-3 flex items-end justify-between"><div><h2 className="text-lg font-black">Ready Match</h2><p className="mt-1 text-xs text-[#7892ac]">Accepted 1-v-1 matches and their dedicated chats.</p></div><button type="button" onClick={()=>router.push("/ready-players")} className="rounded-xl bg-[#167bd1] px-3 py-2 text-xs font-black">Find Ready Players</button></div>{pendingChallenges?<button type="button" onClick={()=>router.push("/ready-players")} className="mb-3 flex w-full items-center gap-3 rounded-2xl border border-[#245b91] bg-[#0a2946] p-3 text-left"><span className="grid size-10 place-items-center rounded-xl bg-[#126bc0]"><Swords size={18}/></span><span className="min-w-0 flex-1"><span className="block text-sm font-black">Match challenges waiting</span><span className="text-xs text-[#9bd3ff]">Open Ready Players to accept or decline.</span></span><Badge count={pendingChallenges}/></button>:null}{matches.length?<div className="space-y-2">{matches.map(m=><button key={m.matchId} type="button" onClick={()=>router.push(`/leaderboard?group=${m.conversationId}`)} className="flex w-full items-center gap-4 rounded-2xl border border-[#18365f] bg-[#071426] p-3 text-left"><MatchUpAvatar profile={m.opponent} size="md" alt={nameOf(m.opponent)}/><span className="min-w-0 flex-1"><span className="block truncate text-sm font-black">{nameOf(m.opponent)}</span><span className="mt-1 block text-xs text-[#7892ac]">1-v-1 Match Chat · {relative(m.createdAt)}</span></span><Badge count={m.unread}/><ChevronRight size={18} className="text-[#5f86a8]"/></button>)}</div>:<div className="rounded-2xl border border-dashed border-[#18365f] p-8 text-center text-sm text-[#7892ac]"><Trophy size={26} className="mx-auto text-[#47a8ff]"/><p className="mt-3 font-black">No accepted matches yet.</p><p className="mt-1 text-xs">Accept a Ready Friend challenge to create a dedicated match chat.</p></div>}</section>:<>
   <section className="mt-7"><div className="mb-3 flex items-end justify-between"><div><h2 className="text-lg font-black">Friends</h2><p className="mt-1 text-xs text-[#7892ac]">People you can start a conversation with.</p></div><button type="button" onClick={()=>router.push("/friends")} className="rounded-xl px-3 py-2 text-xs font-black text-[#9bd3ff]">Add Friends</button></div>{friends.length?<div className="space-y-2">{friends.slice(0,showAll?friends.length:5).map(p=><div key={p.id} className="flex items-center gap-3 rounded-2xl border border-[#18365f] bg-[#071426] p-3"><MatchUpAvatar profile={p} size="md" alt={nameOf(p)}/><div className="min-w-0 flex-1"><p className="truncate text-sm font-black">{nameOf(p)}</p><p className="mt-1 text-xs text-[#7892ac]">Friend on MatchUp</p></div><button type="button" disabled={busy===p.id} onClick={()=>void message(p)} className="rounded-xl bg-[#167bd1] px-3 py-2.5 text-xs font-black disabled:opacity-50">{busy===p.id?"Opening…":"Message"}</button></div>)}</div>:<div className="rounded-2xl border border-dashed border-[#18365f] p-7 text-center text-sm text-[#7892ac]">No accepted friends yet.</div>}{friends.length>5?<button type="button" onClick={()=>setShowAll(v=>!v)} className="mt-3 w-full rounded-xl border border-[#245b91] bg-[#0a2946] px-4 py-3 text-xs font-black text-[#9bd3ff]">{showAll?"Show Less":"See All Friends"}</button>:null}</section>
   <section className="mt-8"><div className="mb-3 flex items-center justify-between"><h2 className="text-lg font-black">Recent Chats</h2><div className="flex items-center gap-2"><Badge count={privateUnread}/>{chats.filter(c=>c.kind!=="group").length>5?<button type="button" onClick={()=>setShowAll(v=>!v)} className="text-xs font-black text-[#9bd3ff]">{showAll?"Show Less":"See All"}</button>:null}</div></div>{chats.filter(c=>c.kind!=="group").length?<div className="space-y-1">{(showAll?chats:chats.slice(0,5)).filter(c=>c.kind!=="group").map(c=><button key={c.id} type="button" onClick={()=>router.push(`/leaderboard?group=${c.id}`)} className="flex w-full items-center gap-3 rounded-2xl px-2 py-3 text-left transition hover:bg-[#071426]"><MatchUpAvatar profile={c.avatar} size="md" alt={c.name}/><span className="min-w-0 flex-1"><span className="block truncate text-sm font-black">{c.name}</span><span className="mt-1 block truncate text-xs text-[#7892ac]">{c.preview}</span></span><span className="flex shrink-0 flex-col items-end gap-2"><span className="text-[10px] text-[#7892ac]">{relative(c.lastAt)}</span><Badge count={c.unread}/></span></button>)}</div>:<div className="rounded-2xl border border-dashed border-[#18365f] p-7 text-center text-sm text-[#7892ac]">No conversations yet.</div>}</section>
  </>}
 </div></main>;
}
