"use client";
import { useEffect,useMemo,useRef,useState } from "react";
import { ArrowLeft, Heart, MessageCircle, Reply, Send, Volume2, VolumeX, X } from "lucide-react";
import { useRouter,useSearchParams } from "next/navigation";
import { createBrowserSupabaseClient } from "../../lib/supabase/client";
type Reel={id:string;authorId:string;author:string;body:string;videoUrl:string;likes:number;liked:boolean;following:boolean;comments:{id:string;author:string;body:string}[]};
export function ReelsViewer(){
 const router=useRouter();const params=useSearchParams();const supabase=useMemo(()=>createBrowserSupabaseClient(),[]);const selectedId=params.get("post")||"";const originTab=params.get("tab")==="following"?"following":"for-you";const originQuery=params.get("q")||"";const originScroll=params.get("scroll")||"0";const [reels,setReels]=useState<Reel[]>([]);const [active,setActive]=useState(0);const [userId,setUserId]=useState("");const [commentsOpen,setCommentsOpen]=useState(false);const [commentText,setCommentText]=useState("");const [muted,setMuted]=useState(true);const videoRefs=useRef<Record<string,HTMLVideoElement|null>>({});
 const feedUrl=`/feeds?tab=${encodeURIComponent(originTab)}&q=${encodeURIComponent(originQuery)}&post=${encodeURIComponent(selectedId)}&scroll=${encodeURIComponent(originScroll)}`;const leave=()=>{setCommentsOpen(false);router.replace(feedUrl);};
 useEffect(()=>{const onKey=(e:KeyboardEvent)=>{if(e.key==="Escape"){if(commentsOpen)setCommentsOpen(false);else leave();}};window.addEventListener("keydown",onKey);return()=>window.removeEventListener("keydown",onKey);},[commentsOpen,feedUrl]);
 useEffect(()=>{let alive=true;(async()=>{const {data:auth}=await supabase.auth.getUser();const uid=auth.user?.id||"";if(alive)setUserId(uid);const {data:rows}=await supabase.from("posts").select("id,author_id,body,created_at,video_duration_seconds").eq("media_kind","video").lte("video_duration_seconds",60).order("created_at",{ascending:false}).limit(100);if(!alive)return;const {data:followRows}=uid?await supabase.from("user_follows").select("following_id").eq("follower_id",uid):{data:[]};const followingIds=new Set((followRows||[]).map((f:any)=>f.following_id));const ids=(rows||[]).map((r:any)=>r.id);if(!ids.length){setReels([]);return;}const [{data:media},{data:likes},{data:comments},{data:profiles}]=await Promise.all([supabase.from("post_media").select("post_id,storage_path,media_type").in("post_id",ids).eq("media_type","video"),supabase.from("post_likes").select("post_id,user_id").in("post_id",ids),supabase.from("post_comments").select("id,post_id,author_id,body").in("post_id",ids).order("created_at",{ascending:true}),supabase.from("profiles").select("id,display_name,username").in("id",Array.from(new Set([...(rows||[]).map((r:any)=>r.author_id),...(comments||[]).map((c:any)=>c.author_id)])))]);const pmap=new Map((profiles||[]).map((p:any)=>[p.id,p]));const cmap=new Map<string,any[]>();(comments||[]).forEach((c:any)=>{const a=pmap.get(c.author_id);const list=cmap.get(c.post_id)||[];list.push({id:c.id,author:a?.display_name||a?.username||"MatchUp Player",body:c.body});cmap.set(c.post_id,list);});const built=(rows||[]).map((r:any)=>{const m=(media||[]).find((x:any)=>x.post_id===r.id);if(!m)return null;const a=pmap.get(r.author_id);return {id:r.id,authorId:r.author_id,author:a?.display_name||a?.username||"MatchUp Player",body:r.body,videoUrl:supabase.storage.from("feed-media").getPublicUrl(m.storage_path).data.publicUrl,likes:(likes||[]).filter((l:any)=>l.post_id===r.id).length,liked:Boolean(uid&&(likes||[]).some((l:any)=>l.post_id===r.id&&l.user_id===uid)),following:followingIds.has(r.author_id),comments:cmap.get(r.id)||[]};}).filter(Boolean) as Reel[];const eligible=originTab==="following"&&uid?built.filter(r=>r.following||r.authorId===uid):built;setReels(eligible);const selectedIndex=eligible.findIndex(r=>r.id===selectedId);setActive(selectedIndex>=0?selectedIndex:0);})();return()=>{alive=false};},[supabase,selectedId,originTab]);
 useEffect(()=>{reels.forEach((r,i)=>{const v=videoRefs.current[r.id];if(!v)return;if(i===active){v.muted=muted;void v.play().catch(()=>{});}else{v.pause();v.currentTime=0;}});},[active,reels,muted]);
 const toggleLike=async()=>{const r=reels[active];if(!r||!userId)return;const result=r.liked?await supabase.from("post_likes").delete().eq("post_id",r.id).eq("user_id",userId):await supabase.from("post_likes").insert({post_id:r.id,user_id:userId});if(!result.error)setReels(x=>x.map((a,i)=>i===active?{...a,liked:!a.liked,likes:a.likes+(a.liked?-1:1)}:a));};
 const toggleFollow=async()=>{const r=reels[active];if(!r||!userId||r.authorId===userId)return;const result=r.following?await supabase.from("user_follows").delete().eq("follower_id",userId).eq("following_id",r.authorId):await supabase.from("user_follows").insert({follower_id:userId,following_id:r.authorId});if(!result.error)setReels(x=>x.map(a=>a.authorId===r.authorId?{...a,following:!r.following}:a));};
 const addComment=async()=>{const text=commentText.trim();const r=reels[active];if(!text||!r||!userId)return;const {data,error}=await supabase.from("post_comments").insert({post_id:r.id,author_id:userId,body:text}).select("id,body").single();if(!error&&data){setReels(x=>x.map((a,i)=>i===active?{...a,comments:[...a.comments,{id:data.id,author:"You",body:data.body}]}:a));setCommentText("");}};
 if(!reels.length)return <main className="fixed inset-0 grid place-items-center bg-black text-white"><div className="text-center"><p className="text-sm text-white/70">No video Reels available.</p><button type="button" onClick={leave} className="mt-4 rounded-full bg-[#167bd1] px-4 py-2 text-xs font-black">Back to Feed</button></div></main>;
 const current=reels[active];return <main className="fixed inset-0 z-[100] bg-black text-white"><div className="pointer-events-none absolute inset-x-0 top-0 z-30 h-28 bg-gradient-to-b from-black/75 to-transparent"/><div className="absolute inset-x-0 top-0 z-40 flex items-center justify-between p-4"><button type="button" onClick={commentsOpen?()=>setCommentsOpen(false):leave} aria-label="Back to feed" className="grid size-10 place-items-center rounded-full border border-white/10 bg-black/45">{commentsOpen?<X size={21}/>:<ArrowLeft size={21}/>}</button><div className="text-center"><span className="font-black">Reels</span><span className="ml-2 text-xs text-white/55">{active+1}/{reels.length}</span></div><button type="button" onClick={()=>setMuted(v=>!v)} aria-label={muted?"Turn sound on":"Mute"} className="grid size-10 place-items-center rounded-full border border-white/10 bg-black/45">{muted?<VolumeX size={19}/>:<Volume2 size={19}/>}</button></div><div className="h-full snap-y snap-mandatory overflow-y-auto overscroll-contain" onScroll={e=>{const i=Math.round(e.currentTarget.scrollTop/e.currentTarget.clientHeight);if(i!==active)setActive(Math.max(0,Math.min(i,reels.length-1)));}}>{reels.map((r,i)=><section key={r.id} className="relative flex h-full min-h-full snap-start items-center justify-center bg-black"><video ref={v=>{videoRefs.current[r.id]=v}} src={r.videoUrl} playsInline loop muted={muted} preload={i===active?"auto":"metadata"} className="h-full w-full object-contain"/><div className="absolute inset-x-0 bottom-0 h-72 bg-gradient-to-t from-black/90 via-black/35 to-transparent"/><div className="absolute inset-x-0 bottom-0 z-20 flex items-end justify-between gap-4 p-5 pb-8"><div className="min-w-0 max-w-[calc(100%-90px)]"><div className="mb-2 flex items-center gap-2"><span className="grid size-9 place-items-center rounded-full bg-white/15 text-[10px] font-black">{r.author.slice(0,2).toUpperCase()}</span><p className="truncate text-sm font-black">{r.author}</p><button type="button" onClick={()=>void toggleFollow()} disabled={!userId||r.authorId===userId} className="rounded-full bg-[#167bd1] px-3 py-1 text-[10px] font-black disabled:opacity-40">{r.authorId===userId?"You":r.following?"Unfollow":"Follow"}</button></div>{r.body?<p className="line-clamp-3 text-sm text-white/90">{r.body}</p>:null}</div><div className="flex shrink-0 flex-col items-center gap-4"><button type="button" onClick={()=>void toggleLike()} className="flex flex-col items-center gap-1"><span className={`grid size-12 place-items-center rounded-full bg-black/45 ${r.liked?"text-[#2497ff]":""}`}><Heart size={22} fill={r.liked?"currentColor":"none"}/></span><span className="text-[11px] font-bold">{r.likes}</span></button><button type="button" onClick={()=>setCommentsOpen(true)} className="flex flex-col items-center gap-1"><span className="grid size-12 place-items-center rounded-full bg-black/45"><MessageCircle size={22}/></span><span className="text-[11px] font-bold">{r.comments.length}</span></button><button type="button" onClick={()=>router.push(`/feeds/post/${r.id}#reply`)} className="flex flex-col items-center gap-1"><span className="grid size-12 place-items-center rounded-full bg-black/45 text-[#70c1ff]"><Reply size={22}/></span><span className="text-[11px] font-bold">Reply</span></button></div></div></section>)}</div>{commentsOpen?<div className="absolute inset-x-0 bottom-0 z-50 max-h-[70vh] rounded-t-[28px] border-t border-white/10 bg-[#08182b]/98 p-4"><div className="mb-3 flex items-center justify-between"><h2 className="font-black">Comments</h2><button type="button" onClick={()=>setCommentsOpen(false)}><X size={19}/></button></div><div className="max-h-[45vh] space-y-3 overflow-y-auto">{current.comments.map(c=><div key={c.id} className="rounded-2xl bg-white/[.06] p-3"><p className="text-xs font-bold">{c.author}</p><p className="mt-1 text-sm text-white/85">{c.body}</p></div>)}</div><div className="mt-3 flex gap-2"><input value={commentText} onChange={e=>setCommentText(e.target.value)} onKeyDown={e=>{if(e.key==="Enter")void addComment()}} placeholder={userId?"Add a comment…":"Sign in to comment"} disabled={!userId} className="min-w-0 flex-1 rounded-full border border-white/10 bg-black/25 px-4 py-3 text-sm"/><button type="button" onClick={()=>void addComment()} disabled={!userId} className="grid size-11 place-items-center rounded-full bg-[#167bd1] disabled:opacity-40"><Send size={17}/></button></div></div>:null}</main>;
}
