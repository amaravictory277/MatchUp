"use client";
import { useEffect, useRef, useState } from "react";
import { Bookmark, ChevronLeft, ChevronRight, Download, Forward, Heart, Link2, MessageCircle, MoreHorizontal, Pencil, Reply, Send, Trash2, Volume2, VolumeX, X } from "lucide-react";
import { formatCount, type Post } from "./data";
import { ContentForwarder } from "../share/content-forwarder";
type FeedCardProps={post:Post;onToggleLike:(id:string)=>void;onToggleFollow:(id:string)=>void;onComment:(id:string,text:string)=>void;onEditComment:(postId:string,commentId:string,text:string)=>void;onDeleteComment:(postId:string,commentId:string)=>void;onShare:(id:string)=>void;onDelete:(id:string)=>void;onEdit:(id:string,caption:string)=>void;onToggleSave:(id:string)=>void;onDownload:(id:string,type:"image"|"video")=>void;onOpenPost:(id:string)=>void;onOpenMedia:(id:string,index:number)=>void;onLongPressVideo?:(id:string)=>void};
export function FeedCard({post,onToggleLike,onToggleFollow,onComment,onEditComment,onDeleteComment,onShare,onDelete,onEdit,onToggleSave,onDownload,onOpenPost,onOpenMedia,onLongPressVideo}:FeedCardProps){const [index,setIndex]=useState(0);const [showComments,setShowComments]=useState(false);const [commentText,setCommentText]=useState("");const [menuOpen,setMenuOpen]=useState(false);const [forwardOpen,setForwardOpen]=useState(false);const [editing,setEditing]=useState(false);const [draft,setDraft]=useState(post.caption);const [commentMenu,setCommentMenu]=useState<string|null>(null);const [editingComment,setEditingComment]=useState<string|null>(null);const [commentDraft,setCommentDraft]=useState("");const [soundOn,setSoundOn]=useState(false);const pressTimerRef=useRef<ReturnType<typeof setTimeout>|null>(null);const pressStartRef=useRef<{x:number;y:number}|null>(null);const longPressTriggeredRef=useRef(false);const movedRef=useRef(false);const videoRef=useRef<HTMLVideoElement|null>(null);const menuRef=useRef<HTMLDivElement|null>(null);const commentMenuRef=useRef<HTMLDivElement|null>(null);const total=post.media.length;useEffect(()=>{if(!menuOpen&&!commentMenu)return;const handler=(e:MouseEvent)=>{const t=e.target as Node;if(menuOpen&&menuRef.current&&!menuRef.current.contains(t))setMenuOpen(false);if(commentMenu&&commentMenuRef.current&&!commentMenuRef.current.contains(t))setCommentMenu(null)};document.addEventListener("mousedown",handler);return()=>document.removeEventListener("mousedown",handler)},[menuOpen,commentMenu]);useEffect(()=>{const video=videoRef.current;if(!video||!post.videoUrl)return;const observer=new IntersectionObserver(([entry])=>{if(entry.isIntersecting){video.muted=true;video.volume=0;setSoundOn(false);void video.play().catch(()=>undefined)}else video.pause()},{threshold:.6});observer.observe(video);return()=>observer.disconnect()},[post.videoUrl]);const cancelPress=()=>{if(pressTimerRef.current){clearTimeout(pressTimerRef.current);pressTimerRef.current=null}};const pointerDown=(e:React.PointerEvent<HTMLElement>)=>{movedRef.current=false;longPressTriggeredRef.current=false;pressStartRef.current={x:e.clientX,y:e.clientY};cancelPress();pressTimerRef.current=setTimeout(()=>{if(!movedRef.current){longPressTriggeredRef.current=true;if(post.videoUrl&&onLongPressVideo)onLongPressVideo(post.id);else setMenuOpen(true)}},600)};const pointerUp=(e:React.PointerEvent<HTMLElement>,mediaIndex:number)=>{cancelPress();if(!longPressTriggeredRef.current&&!movedRef.current)onOpenMedia(post.id,mediaIndex);pressStartRef.current=null;longPressTriggeredRef.current=false};const pointerMove=(e:React.PointerEvent<HTMLElement>)=>{const s=pressStartRef.current;if(s&&Math.hypot(e.clientX-s.x,e.clientY-s.y)>14){movedRef.current=true;cancelPress()}};const pointerCancel=()=>{cancelPress();movedRef.current=true;pressStartRef.current=null;longPressTriggeredRef.current=false};const submitComment=()=>{const text=commentText.trim();if(!text)return;onComment(post.id,text);setCommentText("")};const saveEdit=()=>{const text=draft.trim();if(!text)return;onEdit(post.id,text);setEditing(false)};const saveCommentEdit=(id:string)=>{const text=commentDraft.trim();if(!text)return;onEditComment(post.id,id,text);setEditingComment(null);setCommentDraft("")};const toggleVideoMute=async()=>{const video=videoRef.current;if(!video)return;if(video.muted||video.volume===0){video.muted=false;video.volume=1;setSoundOn(true);try{await video.play()}catch{video.muted=true;video.volume=0;setSoundOn(false)}}else{video.muted=true;video.volume=0;setSoundOn(false)}};const quickComments=post.commentList.slice(0,5);return <article id={`post-${post.id}`} className="surface-card overflow-hidden border-[#153c68]" onContextMenu={e=>e.preventDefault()}><div className="flex items-center gap-3 p-4"><span className="grid size-11 shrink-0 place-items-center rounded-full bg-[linear-gradient(140deg,#1476c9,#35b6ff)] text-xs font-black text-white">{post.author.initials}</span><div className="min-w-0 flex-1"><p className="truncate font-bold text-white">{post.author.name}</p><p className="text-xs text-[#7892ac]">{post.time}</p></div>{post.isOwn?<div className="relative" ref={menuRef}><button type="button" onClick={()=>setMenuOpen(o=>!o)} aria-label="Post options" className="icon-button"><MoreHorizontal size={20}/></button>{menuOpen?<div role="menu" className="absolute right-0 top-11 z-30 w-52 overflow-hidden rounded-2xl border border-[#18365f] bg-[#08182b] p-1.5 shadow-xl"><button type="button" onClick={()=>{setDraft(post.caption);setEditing(true);setMenuOpen(false)}} className="flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-semibold text-white"><Pencil size={16}/>Edit text</button><button type="button" onClick={()=>{setForwardOpen(true);setMenuOpen(false)}} className="flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-semibold text-white"><Forward size={16}/>Forward post</button><button type="button" onClick={()=>{onDelete(post.id);setMenuOpen(false)}} className="flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-semibold text-[#ff8fa1]"><Trash2 size={16}/>Delete post</button></div>:null}</div>:<button type="button" onClick={e=>{e.stopPropagation();onToggleFollow(post.author.id)}} aria-pressed={post.following} className={`rounded-full px-4 py-1.5 text-xs font-bold ${post.following?"border border-[#285277] text-[#b7c9da]":"bg-[#167bd1] text-white"}`}>{post.following?"Unfollow":"Follow"}</button>}</div><div className="relative aspect-[4/3] w-full touch-pan-y select-none overflow-hidden bg-[#061120]" onClick={e=>e.stopPropagation()}>{post.videoUrl?<><video ref={videoRef} src={post.videoUrl} poster={post.media[index]} muted playsInline loop autoPlay preload="metadata" className="absolute inset-0 size-full cursor-pointer object-cover" onPointerDown={pointerDown} onPointerUp={e=>pointerUp(e,0)} onPointerMove={pointerMove} onPointerCancel={pointerCancel} aria-label={`${post.author.name} video`}/><button type="button" onClick={e=>{e.stopPropagation();void toggleVideoMute()}} aria-label={soundOn?"Mute video":"Unmute video"} className="absolute bottom-3 right-3 z-10 grid size-10 place-items-center rounded-full border border-white/20 bg-black/55 text-white">{soundOn?<Volume2 size={18}/>:<VolumeX size={18}/>}</button></>:post.media.map((src,i)=><img key={src+i} src={src||"/placeholder.svg"} alt={`${post.author.name} post media ${i+1}`} draggable={false} className="absolute inset-0 size-full cursor-pointer object-cover transition-opacity duration-300" style={{opacity:i===index?1:0}} onPointerDown={pointerDown} onPointerUp={e=>pointerUp(e,i)} onPointerMove={pointerMove} onPointerCancel={pointerCancel}/>)}{total>1?<><span className="absolute right-3 top-3 rounded-full bg-black/55 px-2.5 py-1 text-[11px] font-bold text-white">{index+1}/{total}</span><button type="button" onClick={e=>{e.stopPropagation();setIndex((index-1+total)%total)}} aria-label="Previous media" className="absolute left-2 top-1/2 grid size-8 -translate-y-1/2 items-center justify-center rounded-full bg-black/45 text-white"><ChevronLeft size={18}/></button><button type="button" onClick={e=>{e.stopPropagation();setIndex((index+1)%total)}} aria-label="Next media" className="absolute right-2 top-1/2 grid size-8 -translate-y-1/2 items-center justify-center rounded-full bg-black/45 text-white"><ChevronRight size={18}/></button></>:null}</div><div className="cursor-pointer p-4" onClick={()=>onOpenPost(post.id)}>{editing?<div onClick={e=>e.stopPropagation()}><textarea value={draft} onChange={e=>setDraft(e.target.value)} rows={3} className="w-full rounded-2xl border border-[#18365f] bg-[#071426] p-3 text-sm text-white"/><button type="button" onClick={saveEdit} className="mt-2 rounded-full bg-[#167bd1] px-4 py-2 text-xs font-bold">Save</button></div>:<p className="text-sm leading-6 text-[#d8e5f0]">{post.caption}</p>}<div className="mt-4 flex items-center gap-5 text-sm font-semibold" onClick={e=>e.stopPropagation()}><button type="button" onClick={()=>onToggleLike(post.id)} className={`flex items-center gap-2 ${post.liked?"text-[#2497ff]":"text-[#b7c9da]"}`}><Heart size={20} fill={post.liked?"currentColor":"none"}/>{formatCount(post.likes)}</button><button type="button" onClick={()=>onOpenPost(post.id)} className="flex items-center gap-2 text-[#b7c9da]" aria-label="Open post comments"><MessageCircle size={20}/>{formatCount(post.comments)}</button><button type="button" onClick={()=>setShowComments(s=>!s)} className="ml-auto flex items-center gap-2 text-[#70c1ff]" aria-label="Quick reply"><Reply size={19}/>Reply</button><button type="button" onClick={()=>onToggleSave(post.id)} className={`flex items-center gap-2 ${post.saved?"text-[#70c1ff]":"text-[#b7c9da]"}`}><Bookmark size={19}/><span className="hidden sm:inline">{post.saved?"Saved":"Save"}</span></button><button type="button" onClick={()=>onShare(post.id)} aria-label="Copy post link" className="text-[#b7c9da]"><Link2 size={19}/></button></div>{showComments?<div className="mt-4 border-t border-[#15304e] pt-4" onClick={e=>e.stopPropagation()}><div className="mb-3 flex items-center justify-between"><p className="text-sm font-bold text-white">Quick replies</p><button type="button" onClick={()=>setShowComments(false)} aria-label="Close quick replies"><X size={16}/></button></div>{quickComments.map(c=><div key={c.id} className="mb-2 rounded-2xl bg-[#0a1b2f] px-3 py-2"><p className="text-xs font-bold text-[#70c1ff]">{c.author}</p><p className="text-sm text-[#d8e5f0]">{c.text}</p></div>)}{!quickComments.length?<p className="text-sm text-[#7892ac]">Be the first to reply.</p>:null}{post.comments>5?<button type="button" onClick={()=>onOpenPost(post.id)} className="mt-2 text-xs font-bold text-[#70c1ff]">View all {post.comments} comments</button>:null}<div className="mt-3 flex gap-2"><input value={commentText} onChange={e=>setCommentText(e.target.value)} onKeyDown={e=>{if(e.key==="Enter")submitComment()}} placeholder="Add a reply..." className="min-w-0 flex-1 rounded-full border border-[#18365f] bg-[#071426] px-4 py-2 text-sm text-white"/><button type="button" onClick={submitComment} className="grid size-9 place-items-center rounded-full bg-[#167bd1] text-white"><Send size={16}/></button></div></div>:null}</div>{menuOpen&&!post.isOwn?<div className="fixed inset-0 z-[80] flex items-end justify-center bg-black/40 p-4 sm:items-center" onClick={()=>setMenuOpen(false)}><div className="w-full max-w-sm rounded-3xl border border-[#194b7c] bg-[#08182b] p-3" onClick={e=>e.stopPropagation()}><button type="button" onClick={()=>{onToggleSave(post.id);setMenuOpen(false)}} className="flex w-full items-center gap-3 rounded-2xl p-4 text-left text-sm font-bold text-white"><Bookmark size={18}/>{post.saved?"Unsave":"Save"}</button><button type="button" onClick={()=>{onDownload(post.id,post.videoUrl?"video":"image");setMenuOpen(false)}} className="flex w-full items-center gap-3 rounded-2xl p-4 text-left text-sm font-bold text-white"><Download size={18}/>Download {post.videoUrl?"Video":"Image"}</button><button type="button" onClick={()=>{setForwardOpen(true);setMenuOpen(false)}} className="flex w-full items-center gap-3 rounded-2xl p-4 text-left text-sm font-bold text-white"><Forward size={18}/>Forward post</button><button type="button" onClick={()=>setMenuOpen(false)} className="flex w-full items-center justify-center rounded-2xl border border-[#18365f] p-3 text-sm font-bold text-[#a9bdd5]">Cancel</button></div></div>:null}<ContentForwarder open={forwardOpen} onClose={()=>setForwardOpen(false)} title={`Post by ${post.author.name}`} contentUrl={`${typeof window!=="undefined"?window.location.origin:""}/feeds/post/${post.id}`}/article>;
}
